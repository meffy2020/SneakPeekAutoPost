// next.config.js

module.exports = {
    images: {
        domains: ['https://sneak-peek-auto-post.vercel.app'], // 여기에 이미지가 호스팅된 도메인을 추가하세요
    },
};


/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['pixabay.com'],
    },
  };
  
  module.exports = nextConfig;
  